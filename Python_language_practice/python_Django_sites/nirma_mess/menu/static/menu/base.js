/*
$(document).ready(function() {
    $(window).scroll(function() {
        const height = $('.navbar').height();
        const scrollTop = $(window).scrollTop();
        if (scrollTop >= height - 80) {
            $('.writing').addClass('solid-nav');
        }
        else {
            $('.writing').removeClass('solid-nav');
        }
    });
});
*/
